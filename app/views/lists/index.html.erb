<h2>Lists</h2>
<ul>
<% @lists.each do |list| %>
	<li><%= link_to list.name, list_path(list) %> | <%= link_to "Edit", edit_list_path(list) %> | <%= link_to "Delete", list_path(list), :method => "delete" %>
<% end %>
</ul>
<div class="actions">
	<%= link_to "Create new list", new_list_path %>
</div>
